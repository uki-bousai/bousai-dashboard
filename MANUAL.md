# 避難所・物資 状況ダッシュボード 運用マニュアル

災害対策本部の担当者（管理者）向けに、このアプリの日々の使い方をまとめたマニュアルです。
初回の公開設定は完了済みのため、このマニュアルは **日常の更新作業** を中心に説明します。

## 1. URL一覧

| ページ | URL | 誰が使う |
|---|---|---|
| 市民向けページ | https://uki-bousai.github.io/bousai-dashboard/ | 誰でも閲覧可 |
| 管理者画面 | https://uki-bousai.github.io/bousai-dashboard/admin.html | 管理者のみ（保存にトークンが必要） |
| GitHubリポジトリ | https://github.com/uki-bousai/bousai-dashboard | 管理者の設定・履歴確認用 |

- 市民向けページは自動で5分ごとにデータを再取得します。市民には市民向けページのURLだけを案内してください。
- 管理者画面のURLを知られても、トークンがなければ保存はできません（閲覧・入力操作だけなら誰でもできます）。

## 2. 仕組み（1分で理解）

```
管理者 ── 管理者画面 ──(保存ボタン＝GitHubへのコミット)──▶ GitHubリポジトリ（data.json）
                                                              │
市民 ◀── 市民向けページ ◀──(5分ごとに自動取得)───────────────┘ (GitHub Pages)
```

- 「保存して公開」を押すたびに、データ（`data.json`）がGitHubに記録されます。**過去の更新履歴はすべて残る**ので、間違えても復旧できます（→ 9章）。
- 保存から市民向けページへの反映までは **1〜2分** かかります（リアルタイムではありません）。

## 3. 管理者になるための準備（管理者ごとに1回）

このアプリの保存方式は2つあり、どちらが有効かは管理責任者（代表者）の設定で決まります。

| 方式 | 管理者に必要なもの | GitHubアカウント |
|---|---|---|
| **合言葉方式**（中継サーバー設定済みのとき） | 名前と合言葉 | **不要** |
| トークン方式（中継サーバー未設定のとき） | GitHubアカウントとトークン | 必要（→ 付録A） |

### 合言葉方式の場合（推奨・かんたん）

1. 管理責任者から自分の**名前**と**合言葉**を受け取る（→ 発行方法は 8章）
2. 管理者画面 https://uki-bousai.github.io/bousai-dashboard/admin.html を開くと**ログイン画面**が表示されるので、名前と合言葉を入力して「ログイン」
   - 「この端末に保存する」にチェックを入れると、次回からログイン画面は出ません
   - **共用端末ではチェックを外してください**
3. これで準備完了。GitHubの登録は不要です

合言葉は中継サーバーにのみ送信され、正しい場合だけサーバーが代理でデータを保存します。

### トークン方式の場合

中継サーバーを使わない構成では、GitHubアカウントとアクセストークンが必要です。手順は **付録A** を参照してください。

## 4. 日々の更新作業（基本の流れ）

1. 管理者画面 https://uki-bousai.github.io/bousai-dashboard/admin.html を開く
   （現在公開中のデータが自動で読み込まれます）
2. 各タブで数値を更新する（→ 5章）
3. 画面下部に「● 未保存の変更があります」と出ていることを確認
4. **「保存して公開」** ボタンを押す
5. 「保存しました」の表示を確認 → **1〜2分後**に市民向けページに反映

- 更新の目安: 避難者数・在庫は **1日1〜数回、決まった時間に**（例: 朝夕）。状況が大きく動いたときは随時。
- 避難者数の推移グラフは、保存するたびに「その日の地区別合計」が自動記録されます。同じ日に複数回保存した場合は最後の値で上書きされるので、1日の代表値を最後に保存する運用がおすすめです。

## 5. 各タブの使い方

### 「避難所」タブ

避難所ごとの受入状況を入力します。

- **定員(人)**: 受入可能な人数
- **避難者(人)**: 現在の避難者数（稼働率は自動計算）
- **緯度・経度**: 地図表示用。行の「**📍検索**」を押すと避難所名から場所の候補を検索し、選ぶだけで自動入力されます（国土地理院とOpenStreetMapの無料検索を利用）。小さな施設は検索に載っていないことも多いので、その場合は「**🗺地図**」を押して地図上で避難所の場所をタップすれば指定できます。空欄の避難所は地図に表示されません
- 「＋ 避難所を追加」で新しい避難所を追加。行末のボタンで削除できます

市民向けページでは、稼働率に応じてマーカー・表示が自動で変わります（青=受入可 / 黄=⚠90%以上で逼迫 / 赤=⛔定員超過 / 灰=データ入力待ち）。

### 「避難所在庫」タブ

避難所を選んで、品目ごとの在庫数を入力します。

- 必要量/日はその避難所の避難者数から自動計算され、**不足かどうかは自動判定**されます
- 不足分は市民向けページの「避難所ごとの不足物資」一覧と、地図のポップアップに自動表示されます
- 品目の追加や「1人1日あたり」の変更は「物資在庫」タブで行います

### 「物資在庫」タブ

品目の一覧（マスタ）と、本部・倉庫の在庫、調達手配中の量を管理します。

- **1人1日あたり**（原単位）を入れると、必要量/日は「市全体の避難者数 × 1人1日あたり」で自動計算されます
  （例: 飲料水6本、食料3食、簡易トイレ0.02基=50人に1基）
- ミルクなど人数から計算できない品目は空欄にして、必要量/日を直接入力します
- **本部在庫**: 本部・倉庫にある在庫 / **手配中**: 発注済みでまだ届いていない量
- **充足率** = (本部在庫 + 避難所在庫計 + 手配中) ÷ 必要量/日（自動計算）
- 「＋ 品目を追加」で品目を追加できます

### 「融通提案」タブ（入力不要・参考情報）

現在の入力内容から、不足している避難所への物資の回し方を自動提案します。

- 本部・倉庫の在庫を優先し、足りない分は在庫に余裕のある避難所からの融通案が表示されます
- **提案どおりに配送したら、「避難所在庫」タブで在庫を付け替えて保存**してください（自動では反映されません）
- このタブは管理者画面だけの表示で、市民向けページには出ません

### 「設定」タブ

- ログイン情報の入力・保存（合言葉方式なら名前と合言葉、トークン方式ならアクセストークン → 3章）
- 「最新データを読み込み直す」: 他の管理者が保存した内容を取り込みたいときに押します

## 6. 複数人で運用するときのルール

- 同時に編集して保存がぶつかった場合、後から保存した人の画面で自動的に最新版を取り直して再保存されます。それでもエラーになったら「最新データを読み込み直す」を押し、自分の変更を入れ直してください
- 事故を減らすため、**時間帯や担当タブ（避難所担当・物資担当など）を分ける**運用をおすすめします
- 編集を始める前に「最新データを読み込み直す」を押す習慣をつけると安全です

## 7. 困ったとき（トラブルシューティング)

| 症状 | 原因と対処 |
|---|---|
| 「名前または合言葉が違います」 | 入力ミスが最多。管理責任者に正しい名前・合言葉を確認する。それでもダメなら管理責任者側で登録（ADMIN_USERS）を確認（→ 8章） |
| ログイン画面で「中継サーバーに接続できません」 | 通信環境を確認。復旧しない場合は管理責任者に連絡（中継サーバーの障害・設定変更の可能性） |
| 保存時に「GitHubへの保存に失敗（HTTP 401/403）」 | 中継サーバーに保管しているトークンの**有効期限切れ**の可能性が大。管理責任者がトークンを作り直して Worker の `GITHUB_TOKEN` を更新する（→ 12章） |
| （トークン方式）保存時に「認証エラー（401）」 | トークンの入力ミス、または**有効期限切れ**。トークンを作り直して入れ直す（→ 付録A） |
| （トークン方式）保存時に「権限がありません（403）」 | トークンの権限不足。Contents が Read and write になっているか、対象リポジトリに `bousai-dashboard` が含まれているか確認 |
| （トークン方式）保存時に「ファイルが見つかりません（404）」 | トークンの Repository access に `bousai-dashboard` が入っていない可能性が高い。トークンを作り直す |
| 保存が競合エラーになる | 他の管理者と同時に保存した。「最新データを読み込み直す」→ 変更を入れ直して再保存 |
| 保存したのに市民向けページが変わらない | 反映まで1〜2分かかります。5分待っても変わらなければ、ページを再読み込み（スマホは引っ張って更新）。それでもダメなら GitHub の `data.json` が更新されているか確認（→ 9章） |
| 地図が表示されない | 避難所の緯度・経度が空欄（入力すれば表示されます）。全体が出ない場合は通信環境の問題で、地図タイル（インターネット上のCDN）に接続できていません。地図以外の表示には影響しません |
| 「● 未保存の変更があります」が消えない | 「保存して公開」を押していません。押さずにページを閉じると入力内容は失われます |
| 管理者画面に古いデータが出る | 「最新データを読み込み直す」を押す |

## 8. 管理者の追加・削除

### 合言葉方式の場合（管理責任者が行います）

管理者の一覧は、中継サーバー（Cloudflare Worker）の環境変数 `ADMIN_USERS` で管理します。

1. https://dash.cloudflare.com/ にログイン → 対象の Worker を開く
2. **Settings** → **Variables and Secrets** → `ADMIN_USERS` の **Edit（値の変更）**
3. JSON形式で名前と合言葉を追加・削除して保存（保存した瞬間に反映されます）

   ```json
   {"田中":"aki-2026-yama","佐藤":"kawa-9-hoshi","鈴木":"umi-33-tsuki"}
   ```

   - 合言葉は**推測されにくいもの**にしてください（10文字以上、名前や「bousai」などは避ける）
   - 人ごとに別の合言葉にすると、保存履歴に「更新者: 名前」が残り、退任時にその人だけ削除できます
4. 追加した人に名前と合言葉を**口頭や紙など安全な方法で**伝える（メールやSNSでの平文送信は避ける）

退任時は、その人の行を JSON から削除して保存するだけです。

### トークン方式の場合

1. https://github.com/uki-bousai/bousai-dashboard/settings/access を開く
2. **Add people** → 追加したい人のGitHubユーザー名を入力
3. Role は **Write** を選択して招待
4. 本人が招待を承諾したら、付録Aの手順で各自トークンを作成

削除（退任時）は、同じ画面（Settings → Collaborators and teams）で該当者を **Remove** し、共用端末にトークンを保存していた場合は「設定」タブでトークン欄を空にして保存チェックを外します。

## 9. データの確認・バックアップ・復旧

保存のたびにGitHubへコミットされるため、**全履歴が自動でバックアップ**されています。

- **更新履歴を確認（かんたん）**: 管理者画面の「**履歴**」タブに、いつ・誰が保存したかが新しい順に表示されます。「変更内容」を押すと、その保存で変わった箇所をGitHub上で確認できます
- **現在のデータを確認**: https://github.com/uki-bousai/bousai-dashboard/blob/main/data.json
- **さらに詳しい履歴**: 上のページで **History** ボタンを押すと、全履歴が一覧できます
- **間違ったデータを戻す**: History から戻したい時点のコミットを開き、その時点の `data.json` の内容（Rawボタンで表示）をコピー。GitHub上で `data.json` を編集（鉛筆アイコン）して貼り付け、Commit changes で保存すると、その内容で公開されます

## 10. 運用上の注意（必ず守ること）

- **個人情報は絶対に入力しない**。リポジトリは public のため、`data.json` の内容と更新履歴は誰でも閲覧できます。避難者名簿・氏名・連絡先などは入れないでください
- 数値は速報値として扱う（市民向けページにもその旨の注意書きがあります）
- 合言葉・トークンは他人に教えない。共用端末では「この端末に保存する」を外す
- （管理責任者）中継サーバーの `GITHUB_TOKEN` の有効期限が近づいたら作り直して差し替える（期限切れが保存エラーの最も多い原因です → 12章）
- リポジトリを private に戻すと市民向けページが表示されなくなるので変更しない

## 11. 将来の拡張

秒単位の即時反映や、入力権限のさらに細かい管理が必要になった場合は、Firebase / Supabase などのバックエンドへの移行を検討してください（データ構造はそのまま使えます）。

## 12. 中継サーバーの初期設定（管理責任者が1回だけ行う）

合言葉方式を有効にするための設定です。無料の Cloudflare Workers を使います。
所要時間は20〜30分程度で、一度設定すれば以後の作業は 8章の管理者追加・削除だけです。

### 12-1. GitHubトークンの作成（中継サーバー保管用・1本だけ)

1. GitHubにログイン → 右上アイコン → **Settings** → 左メニュー最下部 **Developer settings**
2. **Personal access tokens → Fine-grained tokens → Generate new token**
3. 設定:
   - Token name: `bousai-relay` など
   - Expiration: 運用期間に合わせて（期限が切れたらこの手順で作り直して差し替え）
   - Resource owner: `uki-bousai`
   - Repository access: Only select repositories → `bousai-dashboard` のみ
   - Permissions → Repository permissions → **Contents: Read and write**
4. 生成された `github_pat_...` をコピーしておく（次の手順で使います）

### 12-2. Cloudflare Worker の作成

1. https://dash.cloudflare.com/sign-up で無料アカウントを作成（メールアドレスだけで作れます）
2. ダッシュボード左メニューの **Workers & Pages** → **Create** → **Create Worker**
3. 名前を付けて（例: `bousai-relay`）**Deploy** を押す（まずはサンプルのままでOK）
4. **Edit code** を開き、中身を全部消して、このリポジトリの
   [`worker/worker.js`](worker/worker.js) の内容を貼り付けて **Deploy**
5. Worker の **Settings → Variables and Secrets** で次の2つを追加（タイプは **Secret** を選択）:
   - `GITHUB_TOKEN`: 12-1 で作ったトークン
   - `ADMIN_USERS`: 管理者の一覧（JSON形式。例: `{"田中":"aki-2026-yama","佐藤":"kawa-9-hoshi"}`）
6. Worker のURL（`https://bousai-relay.〜.workers.dev`）をメモする

### 12-3. 管理者画面の切り替え

1. GitHub上で `admin.html` を開いて編集（鉛筆アイコン）
2. 冒頭付近の設定を書き換えて Commit changes:

   ```js
   const WORKER_URL = "https://bousai-relay.〜.workers.dev";   // 12-2 でメモしたURL
   ```

3. 1〜2分後、管理者画面を開くとログイン画面が表示されるようになります。
   自分の名前と合言葉でログインし、テスト保存（どこか1箇所変えて保存→元に戻して保存）で動作確認してください

`WORKER_URL` を空文字 `""` に戻せば、いつでも従来のトークン方式に戻せます。

## 付録A. トークン方式での準備（中継サーバーを使わない場合）

1. GitHubアカウントを持っていない場合は https://github.com/ で作成
2. 既存の管理者に依頼し、リポジトリの Collaborator として追加してもらう（→ 8章のトークン方式の手順）
3. 届いた招待メールの「View invitation」から承諾する
4. GitHubにログイン → 右上アイコン → **Settings** → **Developer settings** →
   **Personal access tokens → Fine-grained tokens → Generate new token**
5. 設定:
   - Token name: わかりやすい名前（例: `bousai-dashboard`）
   - Expiration: 運用期間に合わせて（例: 90日）。期限が切れたら作り直す
   - Resource owner: `uki-bousai`
   - Repository access: Only select repositories → `bousai-dashboard` のみ
   - Permissions → Repository permissions → **Contents: Read and write**
6. 生成された `github_pat_...` をコピーし、管理者画面の「設定」タブ →「アクセストークン」欄に貼り付け
   - 「この端末にトークンを保存する」にチェックを入れると次回から入力不要
   - 共用端末ではチェックを外す

トークンはGitHub以外には送信されません。
