# 在宅避難者 物資支援アプリ ─ 設計ドキュメント

避難所へ行くことが難しい**在宅避難者**へ、必要な物資を必要な量だけ届けるための仕組みです。

## 最重要思想

利用者に**数量を判断させない・数えさせない**。

- 在宅避難の実態は、世帯ごとの登録も人数の把握も現場負担が大きすぎる
- だから登録は**区ごとに1件**: 在宅避難の世帯数（概数）と、必要な物資のタップ選択だけ
- 数量はアプリが概算する:
  **3日分の数量 = 1人1日あたり × 世帯数 × 1世帯あたり平均人数（設定値・初期2人）× 目標日数**
- おむつ・ミルクなど対象が限られる品目は、区の乳幼児・高齢者の人数（任意入力）で計算
- 支援者側に見せるのは「必要な物資」「3日分の数量」「届け先（地区の集積場所）」だけ
- 集計は町単位ではなく**公民館ごとの行政区単位**。物資は各地区の集積場所に集める

避難所の物資在庫は自治体管理のため、本アプリでは扱いません（マッチング対象外）。
地域の在宅避難者ニーズと、民間（企業・店舗・団体・個人）からの物資提供を直接つなぎます。

## 採用技術（既存構成を踏襲）

このリポジトリは **静的HTML + JSON + GitHub Pages + Cloudflare Worker中継** で動いています
（サーバーレス・ビルド不要・オフライン対応PWA）。在宅避難者アプリも同じ構成にしました。

```
情報提供者 ── zaitaku-admin.html ──(Worker経由で zaitaku.json をコミット)──▶ GitHub
                                                                             │
支援者・市民 ◀── zaitaku.html（地区別集計・5分ごと自動更新） ◀──────────────┘ (GitHub Pages)
```

- 認証は既存と同じ「名前 + 合言葉」（Cloudflare Worker の `ADMIN_USERS`）
- 保存 = Gitコミットなので**全操作履歴が自動的に残る**（status_history に相当）
- Supabase等への移行が必要になったら、`zaitaku.json` のデータ構造をそのままテーブルに落とせる設計

## ファイル構成

| ファイル | 役割 |
|---|---|
| `zaitaku.html` | 公開ページ。**地区単位の集計のみ**（全体サマリー・必要な物資と3日分の数量） |
| `zaitaku-admin.html` | 入力画面。区ごとの世帯数・必要物資の登録、計算基準の変更（要ログイン） |
| `zaitaku-calc.js` | 計算ロジック（両ページで共有。Nodeでもテスト可能） |
| `zaitaku.json` | データ本体（settings / rules / districts / households） |

## データモデル（zaitaku.json）

```
settings   … buffer_days（何日分を届けるか=3）, disaster_end_date, water_outage_end_date
rules      … 計算基準マスター（supply_rules に相当）
             id / name / unit / daily_amount_per_person / target(all|infant|elderly|…)
             / buffer_days(空=共通設定) / is_active / packs（なじみのある単位への換算表。
             例: 36L →「2Lペットボトル 約18本」の補足表示に使う）
districts  … 地区（行政区）ごとの登録 [{name, site, site_hours?, staff?, report}]。区ごとに1件。
             site_hours は集積場所の受付時間（任意・自由記述。例「9:00〜17:00」）。
             公開ページでは集積場所の下に「🕐受付時間」として表示
             name は「町名 校区名 区名」の空白区切り（例: 松橋町 豊福校区 豊福区。
             校区は省略可 ─ 豊野町は校区の区分がないため「豊野町 上糸石」の2語になる）。
             公開ページは 町 → 校区 → 区 の階層で表示する。
             site は集積場所（公民館など）。「📍集積場所」として表示。
             staff は物資を届ける担当（市議会議員ペアなど・任意）。「🤝担当」として
             公開ページに表示。**氏名のみ**（電話番号・住所は保存しない）

report     … その区の報告 { reporter_role, households（在宅避難の世帯数・概数）,
             support_households（うち要支援の世帯数・任意）,
             infants, elderly（任意・おむつやミルク用）, needs, notes, updatedAt }
             households は在宅避難の世帯数（**分からなければ 0＝未報告**でよい）。
             support_households は高齢・介助が必要などで**支援の手が要る**世帯の数。
             どちらか一方だけでも登録できる（両方未入力は不可）。
             **物資はどの世帯にも必要なので、数量は households（在宅避難）で計算する。**
             要支援ぶんの数量（needSupport）は「優先して届ける分」として併記するだけで、
             計算の基準にはしない
needs      … 必要な物資 { 品目id: { note: "Mサイズ 3人" } }（タップで選択。数量は持たない）
             必要量は保存せず毎回計算（基準変更が即反映される）
households … 旧形式との互換用（現在は常に空配列）
```

第2フェーズで追加予定: `supplies`（民間提供）, `allocations`（割当）, `delivery_tasks`（配送）。
`zaitaku-admin.html` の保存処理は未知のキーをそのまま引き継ぐため、追加しても壊れません。

## 計算ロジック（zaitaku-calc.js）

1. **対象人数（概算）**: `target=all` は 在宅避難の世帯数（未報告なら要支援の世帯数）× 1世帯あたり平均人数（設定・初期2。在宅避難は高齢世帯が中心のため少なめに設定。地域の実態に合わせて変更可）。
   乳幼児・高齢者向け品目は区の報告人数（未入力なら1人とみなす）
2. **1日必要量** = 1人1日あたり × 対象人数
3. **3日分の数量** = 1日必要量 × 目標日数（初期値3日）。表示は切り上げ
4. 「必要」と報告された品目だけを計算・表示する。残量・残り日数・不足量は扱わない
   （在宅避難では在庫や人数の正確な把握は現実的でないため、すべて概算で割り切る）
5. 断水終了予定日までの全量を一括調達はしない。**3日分を届け、また必要になったら
   区長が品目を選び直す**（届いたら解除する）繰り返し方式
6. **集計**: 区ごとに品目別の3日分数量。全体では品目ごとに地区数・世帯数・数量を合算。
   公開ページには**必要と報告された物資だけ**を表示する

### 初期基準値（1人1日あたり。すべて入力画面の「計算基準の設定」から変更可能）

| 品目 | 基準値 | 対象 | 表示単位（換算） |
|---|---|---|---|
| 飲料水 | 3L | 全員 | 2Lペット・500mlペット・L |
| パックご飯 | 3個（1食1個） | 全員 | 個・3個パック |
| レトルト食品 | 3個（1食1個） | 全員 | 個 |
| お茶 | 0.5L | 全員 | 500mlペット・2Lペット |
| スポーツドリンク | 0.5L | 全員 | 500mlペット・2Lペット・粉末(1L分) |
| お菓子 | 1個・袋 | 全員 | 個・袋 |
| トイレットペーパー | 0.15ロール（約1週間で1ロール） | 全員 | ロール・12ロールパック |
| お尻拭き | 30枚 | 乳幼児 | 枚・80枚パック |
| 身体を拭くシート | 2枚 | 全員 | 枚・30枚パック |
| 子ども用紙おむつ | 5枚 | 乳幼児 | 枚（サイズをメモ可） |
| 大人用紙おむつ | 3枚 | 高齢者 | 枚（サイズをメモ可） |
| ミルク | 800ml | 乳幼児 | 粉g（×7.5ml換算）・液体240ml缶 |

品目の追加・無効化は `zaitaku.json` の `rules` で行う（使わない品目は入力画面の
「計算基準」でチェックを外せば入力欄からも消える）。

## 個人情報方針

- `zaitaku.json` は**公開リポジトリにコミットされる**ため、氏名・電話番号・詳細住所・病名は
  **最初から保存しない**設計（入力画面にも明記）
- 登録は区単位の概数のみで、**世帯を特定する情報はそもそも存在しない**
- 公開ページは地区単位の集計のみ表示
- 「物資を届ける担当」（staff）は市議会議員など公職者の**氏名のみ**を想定。
  議員名簿にある電話番号・住所はアプリに保存しない
- 個別配送に必要な住所等を扱う場合は、公開JSONでは不可能なため Supabase 等への移行が前提（第2フェーズ）

## 地区名の入力（公式の行政区一覧に準拠）

- 町名・校区/地区名・区名はすべてドロップダウン。選択肢は宇城市の**行政区一覧**に準拠:
  - 三角町は校区でなく **5つの地区**（三角・みなと・戸馳・郡浦・大岳）
  - 不知火町は **不知火校区・松合校区** の2つ
  - **豊野町は校区の区分なし**（区名は「豊野町 上糸石」のように2語で保存）
- 一覧にない区名は「**その他（手入力）**」で自由入力できる（合併・呼び名違い・
  仮設団地などに対応）。旧データの区名も自動的に「その他」として読み込まれる
- リストは `zaitaku-admin.html` の `SCHOOL_DISTRICTS` / `WARDS` 定数。訂正はここを直す

## MVP実装順序

- [x] **フェーズ1（実装済み）**: 区ごとの世帯数登録 / 必要物資の選択登録 /
      3日分数量の自動計算（世帯数×平均人数） / 地区別集計 / 検証環境 / 計算基準の管理
- [ ] **フェーズ2**: 支援募集一覧（「この物資を提供できます」ボタン）/ 民間物資提供登録
      （supplies）/ マッチング候補表示 / マッチング確定（allocations・remaining_quantity管理）
- [ ] **フェーズ3**: 配送管理（delivery_tasks）/ 配送完了→必要物資の解除→再計算の循環
- [ ] **将来**: 地図表示・配送最適化・LINE/SMS通知・需要予測

## 権限（運営スタッフ／地区担当者）

| | 運営スタッフ（ADMIN_USERS） | 地区担当者（ZAITAKU_USERS） |
|---|---|---|
| 区の状況（世帯数・必要物資）の登録・更新 | 全地区 | **担当地区のみ** |
| 集積場所の変更 | 全地区 | 担当地区のみ |
| 地区の追加・名前変更・削除 | ○ | ✕ |
| 計算基準・設定の変更 | ○ | ✕ |
| 避難所ダッシュボード等の保存 | ○ | ✕（zaitaku.json のみ） |

- 地区担当者のアカウントは2通りの作り方がある（権限はどちらも同じ）:
  1. **自己登録（招待コード方式・推奨）**: 区の担当者がログイン画面の
     「アカウントを作成する」から、招待コード＋名前＋合言葉＋担当地区を
     入力して自分で作る。合言葉はハッシュ化してCloudflare KVに保存される
  2. **運営が手動登録**: Worker の Secret `ZAITAKU_USERS` に記載:
     `{"山田": {"password":"合言葉", "districts":["松橋町 豊福"]}, ...}`（複数地区も可）
- 制限は**サーバー側（Worker）で強制**される。画面側の出し分けは補助で、
  改造したクライアントから送っても担当外の差分はWorkerが403で拒否する
- 入力画面は /auth の応答（scope）に応じて、担当外の編集ボタンや
  計算基準カードを自動的に隠し、地区名も担当地区で固定する

## 町の統括担当者（複数の区をまとめて代行入力）

区長さんの代わりに入力する人を、**町ぜんぶの担当**として登録できる。

`ZAITAKU_USERS` の `districts` の書き方で、担当できる範囲が3段階になる。

| districts の書き方 | 担当できる範囲 |
|---|---|
| `["豊野町 下安見"]`（区まで） | その区だけ |
| `["豊野町"]`（町名だけ） | その町のすべての区（新しい区の追加も可）。計算基準は不可 |
| `[]`（**空**） | 在宅避難データの全体担当。すべての区＋計算基準・設定も変更できる |

- いずれも `zaitaku.json` / `zaitaku-demo.json` 以外は保存できない
  （避難所ダッシュボード等は運営スタッフ `ADMIN_USERS` のみ）
- **自己登録（招待コード）では、町名だけ・空のどちらも指定できない**（担当地区は必須で、
  町名＋区名の形のみ）。町の統括担当者と全体担当は、運営が `ZAITAKU_USERS` に
  書いて作る場合だけ（招待コードを知る誰もが広い権限を取れてしまうのを防ぐため）

設定例（Cloudflare の Secret `ZAITAKU_USERS`）:

```json
{"豊野-統括": {"password": "合言葉", "districts": ["豊野町"]},
 "代行入力":  {"password": "合言葉", "districts": []}}
```

統括にできるのは Worker の `TOWNS`（三角町・不知火町・松橋町・小川町・豊野町）に
一致する文字列だけ。それ以外の1語（例「両仲間区」）は完全一致の担当としてのみ扱い、
前方一致は起きない。判定は `districtInScope()`。入力画面も同じ規則で編集可否を出し分ける。

## アカウントの自己登録（招待コード方式）

区の担当者が自分でアカウントを作れる仕組み。運営が承認する手間はない。

**流れ**: 運営が招待コードを決めて区長会などで配る → 担当者はログイン画面の
「アカウントを作成する」で 招待コード・名前・合言葉・担当地区 を入力 →
すぐ使える（担当地区だけ編集可）。

**有効にするための初期設定（Cloudflareダッシュボードで1回だけ）**:

1. Storage & Databases → KV → Create namespace（名前は例: `bousai-users`）
2. Workers & Pages → `bousai-relay` → Settings → Bindings → Add →
   KV Namespace → Variable name を **`USERS_KV`** にして 1 のネームスペースを選択
3. Settings → Variables and Secrets → Secret **`INVITE_CODE`** を追加
   （値が招待コードそのもの。例: `uki-2026-bousai`）
4. `worker/worker.js` を再デプロイ

**災害初動などで招待コードをなくす場合**: 変数 **`SIGNUP_OPEN`** を追加して値を
`1` にすると、コードなしで誰でもアカウントを作れる（入力画面の招待コード欄も
自動的に消える）。戻すときは `SIGNUP_OPEN` を削除するか `1` 以外にするだけ。
誰でも作れる状態でも、担当地区は本人が選んだ区に限られ、保存はすべて
Gitコミットとして名前つきで残るため、不審な変更は特定・巻き戻しができる。

**運用**:

- 招待コードが漏れた疑い → `INVITE_CODE` の値を変えるだけ（作成済みアカウントは影響なし）
- アカウント削除 → KVダッシュボードで `user:<名前>` のキーを削除
- 未設定のままでも既存のログインは正常動作（アカウント作成だけ「利用できません」と案内される）
- 保存の記録（Gitコミット）に更新者名が残るため、名前は「地区名＋名字」を推奨

## セキュリティ

- 入力画面の保存は Worker の「名前+合言葉」認証（総当たり対策の遅延あり）
- Worker 側で保存先パスをホワイトリスト化し、データ形状を検証
- DOM生成は `textContent` ベース（innerHTML不使用）で XSS を回避
- SQLは使用しない（静的JSON）。保存はすべてGitコミット＝操作履歴が残る
- 個人情報は設計上保存しない（上記方針）

## 検証環境（練習用）

URLに `?env=demo` を付けると、本番とは別のデータ（`zaitaku-demo.json`）で
動く検証環境になります。画面上部に紫の「🧪検証環境」バナーが出ます。

- 公開ページ: `zaitaku.html?env=demo`
- 入力画面: `zaitaku-admin.html?env=demo`

検証環境の特徴:

- **保存も本番と同じ仕組みで実際に動く**（Worker経由・要ログイン・地区担当者の
  権限チェックも同じ）。区長さんの操作練習・研修に使える
- 本番の `zaitaku.json` には一切影響しない
- 宇城市5町・12地区・約95世帯分の仮データ入り（すべて架空の内容）
- 仮データを初期状態に戻すには、Gitで `zaitaku-demo.json` を元のコミットに戻す

## 運用開始前に必要な作業

1. **Cloudflare Worker の更新**: `worker/worker.js` を再デプロイしてください
   （`zaitaku.json` への保存許可と地区担当者の権限チェックを追加したため。
   未更新だと入力画面の保存が「データの形式が不正です」エラーになります）
2. **地区担当者の自己登録を使う場合**: KVバインディング `USERS_KV` と
   Secret `INVITE_CODE` を設定（上の「アカウントの自己登録」参照）。
   手動で足す場合は Secret `ZAITAKU_USERS`（形式は worker.js 冒頭のコメント参照）。
   運営スタッフは従来どおり `ADMIN_USERS` のままでよい
3. 動作確認: `zaitaku-admin.html` でログイン → テスト世帯を登録 → 保存 →
   1〜2分後に `zaitaku.html` に反映されることを確認

## 想定される課題

- **個別世帯への配送は扱わない**: 登録が区単位の概数のため。物資は集積場所
  （公民館）まで届け、そこから先は区長経由の受渡運用。世帯単位の管理が
  必要になった時点で Supabase + RLS へ移行
- **数量はあくまで概算**: 世帯数 × 平均人数の掛け算。過不足は「届いたら解除、
  足りなければまた選ぶ」の繰り返しで吸収する
- **同時編集の競合**: Worker側で1回リトライするが、複数人が同時保存すると
  後勝ちになる恐れ。区ごとに担当を分ければ実質衝突しない
- **反映まで1〜2分**: GitHub Pages の制約。災害時の物資調整には十分な速さ
- **乳幼児・高齢者の人数が未入力の区**: おむつ・ミルクは1人分で概算される。
  正確にしたい区は人数を入力する（入力画面に案内あり）
- **「届いた」の反映が手動**: 物資が届いたら区長が品目を解除する運用。
  第3フェーズの配送管理で自動化する
